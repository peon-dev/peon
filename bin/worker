#!/bin/bash
set -e

trap "trap - SIGTERM && kill -- -$$" SIGINT SIGTERM EXIT

while true; do
    echo 'tick'   # Will be something like bin/console messenger:consume
    sleep 1
done &

bin/console peon:worker:run -vv
