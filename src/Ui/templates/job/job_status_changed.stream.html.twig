<turbo-stream action="replace" target="job-status-{{ job.jobId }}">
    <template>
        {{ include('job/_job_status.html.twig') }}
    </template>
</turbo-stream>

<turbo-stream action="remove" target="job-{{ job.jobId }}-pending-spinner"></turbo-stream>
